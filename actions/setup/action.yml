name: Setup
description: Harden runner, install mise, and optionally cache Go modules

inputs:
  go-cache:
    description: Cache Go modules (~/dev/go/pkg/mod + ~/.cache/go-build)
    default: 'false'

runs:
  using: composite
  steps:
    - name: Harden the runner (Audit all outbound calls)
      uses: step-security/harden-runner@a90bcbc6539c36a85cdfeb73f7e2f433735f215b # v2.15.0
      with:
        egress-policy: audit

    - name: Install mise
      uses: step-security/mise-action@d65cf9dcb78b983ae2530c89fd28e72286713505 # v3.5.1
      with:
        install: true
        cache: true

    - name: Cache Go modules
      if: inputs.go-cache == 'true'
      uses: actions/cache@cdf6c1fa76f9f475f3d7449005a359c84ca0f306 # v5.0.3
      with:
        path: |
          ~/dev/go/pkg/mod
          ~/.cache/go-build
        key: go-${{ runner.os }}-${{ hashFiles('go.sum') }}
        restore-keys: go-${{ runner.os }}-
